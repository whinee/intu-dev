id: sample-channel
enabled: true

listener:
  type: http
  http:
    port: 8080

validator:
  runtime: node
  entrypoint: validator.ts

transformer:
  runtime: node
  entrypoint: transformer.ts

destinations:
  - kafka-output
